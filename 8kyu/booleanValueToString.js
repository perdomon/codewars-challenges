// 8kyu Convert Boolean values to string values 'yes' and 'no'

// Accept a boolean value and return the corresponding value as a string

function boolToWord( bool ){
  return(bool ? 'Yes' : 'No')
}

// Ternary operator condition ? ifTrue : ifFalse
